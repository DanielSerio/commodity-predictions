# TODO

- [ ] Add a "daily" button that fetches the days price from the MetalPrice API. It will be "daily" because once clicked, it will fetch the days prices, update the database, and then disappear until the next day.
